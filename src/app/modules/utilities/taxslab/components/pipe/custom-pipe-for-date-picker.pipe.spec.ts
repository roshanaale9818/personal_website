import { CustomPipeForDatePickerPipe } from './custom-pipe-for-date-picker.pipe';

describe('CustomPipeForDatePickerPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipeForDatePickerPipe();
    expect(pipe).toBeTruthy();
  });
});
