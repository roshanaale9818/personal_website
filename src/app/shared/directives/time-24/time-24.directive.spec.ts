import { Time24Directive } from './time-24.directive';

describe('Time24Directive', () => {
  it('should create an instance', () => {
    const directive = new Time24Directive();
    expect(directive).toBeTruthy();
  });
});
